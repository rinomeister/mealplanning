# Normalize line endings to LF in the repo (keeps shell scripts working in Linux containers).
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.ico binary
*.jpg binary
*.jpeg binary
*.webp binary
